#include <stdint.h>
#include <stdio.h>
#include <sring.h>



int main(int argc,char* argv[])
{
	uint8_t length=0;
	char array[30]={0};

	printf("Enter the length of an array of numbers [Max 30].\n");
	scanf("%d",&length);

	for(i=0;i<length;i++)
	{
		s
	}

	return 0;
}
